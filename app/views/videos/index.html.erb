  <tbody>

    <div class="container">
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
        <% @videos.each do |video| %>
            <div class="col-md-4">
              <div class="card mb-4 box-shadow">
                <%= image_tag video.thumbnail, :class => "card-img-top", style: "width:100%; height:225px;" if video.thumbnail.attached?%>
                <div class="card-body">
                  <p class="card-text" style="font-size:1.3em;"><%= video.title %></p>
                  <p class="card-text" style="font-weight:300;">
                    <% if video.user.profilePic.attached? %>
                      <%= image_tag video.user.profilePic, :class=>"profileIMG" %>
                    <% else %>
                      <%= image_tag "blank_profile.png", :class=>"profileIMG" %>
                    <% end %>
                    <%= link_to video.user.name, user_user_profile_path(video.user.id), :style=>"color:black;decorations:none;" %></p>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group">
                      <%= link_to 'Watch', video, :class => "btn btn-sm btn-outline-secondary" %>
                    </div>
                    <small class="text-muted"><%= time_ago_in_words(video.created_at) %> ago</small>
                  </div>
                </div>
              </div>
            </div>
        <% end %>
      </div>
    </div>
        <!-- <td><%#= video.description %></td>
        <td><%#= link_to 'Show', video %></td>
        <td><%#= link_to 'Edit', edit_video_path(video) %></td>
        <td><%#= link_to 'Destroy', video, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->

  </tbody>
